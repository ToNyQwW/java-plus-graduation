server:
  port: 0

grpc:
  server:
    port: -1

spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/comment
    username: comment
    password: comment
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always