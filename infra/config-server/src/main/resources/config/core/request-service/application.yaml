server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5439/request
    username: request
    password: request
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always